{
  "name": "jasoti",
  "version": "0.1.0",
  "description": "JASOTI - JSON As Source Of Truth for Interfaces. A Figma plugin that generates components from JSON schema.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
